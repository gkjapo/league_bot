services:
  bo7bot:
    build: .
    container_name: bo7bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data   # sqlite DB persists here
    network_mode: "host"   # discord bots work best without NAT
